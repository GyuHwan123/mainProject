# 영수증 정답 데이터 검증 결과 (2026-09-02)

## 대상

- `영수증 test`: 20건
- `receipt_dataset_verified`: 54건

## 적용 정책

- `할인액 = Σ(품목 단가 × 수량) - 총 결제액`
- 품목의 `금액`은 해당 품목 할인이 반영된 실결제 금액일 수 있다.
- 할인 없음/판독 불가는 `할인액: null`로 명시한다.
- 이미지에 상호·일자가 없으면 값을 추측하지 않고 `null`로 명시한다.
- 영수증에 별도 행으로 출력된 증정품은 단가·금액 0인 품목으로 포함한다.

## 수정 내용

| 이미지 | 수정 내용 |
|---|---|
| `receipt_014.jpg` | 할인액 `null → 4,850` |
| `receipt_018.jpg` | 할인액 `null → 30,970` |
| `receipt_023.jpg` | 할인액 `515 → 2,060`; 코젤 품목 금액 `2,990 → 2,475` |
| `receipt_026.jpg` | 할인액 `7,700 → 35,200`; 누락된 구매일자 키를 `null`로 추가 |
| `receipt_043.jpg` | 할인액 `7,600 → 39,700` |
| `receipt_048.jpg` | 누락된 한라봉 증정품 추가, 총 수량 `7 → 8`, 잘못 읽힌 상품명 두 건 보정 |
| `receipt_36.jpg` | 이미지에 상호가 없어 누락 키를 `가게명: null`로 추가 |
| `receipt_047.jpg` | 이미지에 상호가 없어 누락 키를 `가게명: null`로 추가 |

`receipt_001.jpg`는 이미지의 총할인금액 300원과 현재 JSON의 `할인액: 300`이 일치한다.

두 데이터셋의 모든 레코드에 `할인액` 키가 존재하도록 통일했다. 할인이 확인되지 않는 영수증은 `null`이다.

## 검증 결과

- JSON 문법 오류: 0건
- 이미지 ID 중복: 0건
- 품목 수량 합계 불일치: 0건
- 품목이 있는 영수증의 `정가 합계 - 할인액 = 총 결제액` 불일치: 0건
- 품목이 없는 서비스형 영수증은 위 품목 산식을 적용하지 않았다.

## 백업

- `test01_test20_ground_truth_receipts_taxonomy.before_validation_20260902.json`
- `receipts.before_validation_20260902.json`
