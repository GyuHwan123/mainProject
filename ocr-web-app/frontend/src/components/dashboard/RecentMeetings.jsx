import { useState } from 'react';import { FiArrowRight,FiEdit2,FiFileText,FiPlus,FiTrash2,FiUsers,FiZap } from 'react-icons/fi';
import { extractMeetingActions } from '../../features/dashboardService';import DashboardModal from './DashboardModal';
export default function RecentMeetings({meetings,selectedMeeting,onSelectMeeting,onAdd,onUpdate,onDelete,onAddTask,onAddEvent}){
 const[showAll,setShowAll]=useState(false);const[detail,setDetail]=useState(null);const[editing,setEditing]=useState(null);const[deleting,setDeleting]=useState(null);const[extraction,setExtraction]=useState(null);const[saving,setSaving]=useState(false);
 const openDetail=(item)=>{setDetail(item);onSelectMeeting(item)};const meetingAt=(item)=>{if(item.date)return `${item.date.replaceAll('.','-')}T14:00`;const now=new Date();return `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-${String(now.getDate()).padStart(2,'0')}T14:00`};
 const submit=async(event)=>{event.preventDefault();setSaving(true);const form=new FormData(event.currentTarget);const values={title:form.get('title'),meetingAt:form.get('meetingAt'),participants:form.get('participants').split(',').map((v)=>v.trim()).filter(Boolean),summary:form.get('summary'),content:form.get('content'),status:form.get('status')};try{editing.id?await onUpdate(editing.id,values):await onAdd(values);setEditing(null)}finally{setSaving(false)}};
 const analyze=async(item)=>{setSaving(true);try{setExtraction({meeting:item,...await extractMeetingActions(item.id)});setDetail(null)}finally{setSaving(false)}};
 const accept=async(type,item)=>{setSaving(true);try{if(type==='task')await onAddTask({...item,status:'TODO'});else await onAddEvent(item);setExtraction((old)=>({...old,[type==='task'?'tasks':'schedules']:old[type==='task'?'tasks':'schedules'].filter((value)=>value!==item)}))}finally{setSaving(false)}};
 const remove=async()=>{setSaving(true);try{await onDelete(deleting.id);setDeleting(null)}finally{setSaving(false)}};
 return <><article className="dashboard-card meetings-card"><div className="card-heading"><div><span className="card-icon"><FiFileText/></span><div><h2>최근 회의록</h2><p>회의에서 업무와 일정을 연결하세요.</p></div></div><div className="heading-actions"><button className="text-button" onClick={()=>setShowAll(true)}>전체 보기 <FiArrowRight/></button><button className="icon-button" onClick={()=>setEditing({status:'DRAFT'})}><FiPlus/></button></div></div><div className="meeting-list">{meetings.slice(0,5).map((item)=><button key={item.id} className={selectedMeeting?.id===item.id?'active':''} onClick={()=>openDetail(item)}><div className="meeting-meta"><span>{item.date}</span><em>{item.tag}</em></div><h3>{item.title}</h3><p>{item.summary}</p><div className="meeting-footer"><span><FiUsers/> {item.participants}</span><b>회의록 보기 <FiArrowRight/></b></div></button>)}</div></article>{showAll&&<DashboardModal title="전체 회의록" onClose={()=>setShowAll(false)}><div className="all-meetings">{meetings.map((item)=><button key={item.id} onClick={()=>{setShowAll(false);openDetail(item)}}><span>{item.date}</span><div><strong>{item.title}</strong><p>{item.summary}</p></div><em>{item.tag}</em></button>)}</div></DashboardModal>}{detail&&<DashboardModal title={detail.title} description={detail.date} onClose={()=>setDetail(null)}><div className="meeting-detail"><p><b>참석자</b>{detail.participants}</p><p><b>회의 요약</b>{detail.summary}</p>{detail.content&&<p><b>회의 내용</b>{detail.content}</p>}<div className="detail-actions"><button className="agent-analyze-button" onClick={()=>analyze(detail)} disabled={saving}><FiZap/> AI 업무·일정 추출</button><button className="primary-button" onClick={()=>{setEditing(detail);setDetail(null)}}><FiEdit2/> 수정</button><button className="danger-button" onClick={()=>{setDeleting(detail);setDetail(null)}}><FiTrash2/> 삭제</button></div></div></DashboardModal>}{editing&&<DashboardModal title={editing.id?'회의록 수정':'회의록 추가'} onClose={()=>setEditing(null)}><form className="dashboard-form" onSubmit={submit}><label>회의 제목<input name="title" required defaultValue={editing.title||''}/></label><div><label>회의 일시<input name="meetingAt" type="datetime-local" required defaultValue={meetingAt(editing)}/></label><label>상태<select name="status" defaultValue={editing.status||'DRAFT'}><option value="DRAFT">작성 중</option><option value="CONFIRMED">확정</option><option value="ARCHIVED">보관</option></select></label></div><label>참석자<input name="participants" defaultValue={editing.participants||''}/></label><label>요약<textarea name="summary" rows="3" defaultValue={editing.summary||''}/></label><label>회의 내용<textarea name="content" rows="6" defaultValue={editing.content||''}/></label><footer><button type="button" onClick={()=>setEditing(null)}>취소</button><button className="primary-button" disabled={saving}>저장</button></footer></form></DashboardModal>}{extraction&&<DashboardModal title="AI가 발견한 항목" description={extraction.meeting.title} onClose={()=>setExtraction(null)}><div className="extraction-results">{extraction.tasks.map((item,index)=><section key={`task-${index}`}><em>할 일</em><strong>{item.title}</strong><p>{item.assignee||'담당자 미정'} · {item.due||'마감일 미정'}</p><button onClick={()=>accept('task',item)} disabled={saving}>할 일에 추가</button></section>)}{extraction.schedules.map((item,index)=><section key={`schedule-${index}`}><em>일정</em><strong>{item.title}</strong><p>{item.date} {item.time}</p><button onClick={()=>accept('schedule',item)} disabled={saving}>캘린더에 추가</button></section>)}{!extraction.tasks.length&&!extraction.schedules.length&&<p className="empty-tasks">발견된 업무나 일정이 없습니다.</p>}</div></DashboardModal>}{deleting&&<DashboardModal title="회의록 삭제" onClose={()=>setDeleting(null)}><div className="delete-confirm"><p>‘{deleting.title}’ 회의록을 삭제할까요?</p><footer><button onClick={()=>setDeleting(null)}>취소</button><button className="danger-button" onClick={remove}>삭제</button></footer></div></DashboardModal>}</>
}
